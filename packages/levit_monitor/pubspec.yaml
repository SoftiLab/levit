name: levit_monitor
description: A unified observability and diagnostics engine for the Levit ecosystem.
repository: https://github.com/softilab/levit
version: 0.0.4

environment:
  sdk: ^3.0.0

dependencies:
  # Levit Core
  levit_reactive: ^0.0.4
  levit_dart: ^0.0.4

  # Transports
  web_socket_channel: ^3.0.3
  logger: ^2.6.2
  
  # Annotations
  meta: ^1.17.0

dev_dependencies:
  test: ^1.24.0
  lints: ^6.0.0
  rxdart: ^0.28.0
  stream_channel: ^2.1.4

